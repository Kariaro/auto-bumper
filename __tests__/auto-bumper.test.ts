const Auto = require('@auto-it/core');
const AutoBumper = require('../src');

describe('Auto-Bumper Plugin', () => {
  test('should do something', async () => {
    
  });

  describe('getVersion', () => {
    test('test if version is read', async () => {
      
    });
  });
});
